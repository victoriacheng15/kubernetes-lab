apiVersion: v1
kind: Service
metadata:
  name: web-external
  labels:
    app: web-app
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080 # Explicitly requests static high-range port 30080
    protocol: TCP
