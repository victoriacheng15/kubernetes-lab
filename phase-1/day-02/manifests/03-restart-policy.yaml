apiVersion: v1
kind: Pod
metadata:
  name: restart-demo
  labels:
    app: restart-demo
spec:
  restartPolicy: Always
  containers:
  - name: failing-app
    image: busybox:1.36
    command: ['sh', '-c', 'echo "Starting application..."; sleep 5; echo "Encountered a fatal error!"; exit 1']
