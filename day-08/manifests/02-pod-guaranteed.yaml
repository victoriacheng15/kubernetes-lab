apiVersion: v1
kind: Pod
metadata:
  name: guaranteed-pod
  namespace: resource-lab
spec:
  containers:
  - name: web
    image: nginx:alpine
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 100m
        memory: 128Mi
